
<div class="form-group">
    <div class="col-sm-10 col-sm-offset-2">
        <div class="alert alert-warning">
            <i class="fa fa-exclamation-circle"></i> <strong>Внимание!</strong> Html разметка не поддерживается, только текст!
        </div>
    </div>
</div>
<div class="form-group">
    <div class="col-sm-10 col-sm-offset-2">
        <a href="" id="thumb-image" data-toggle="image" class="img-thumbnail"><img src="{{ thumb }}" alt="" title="" data-placeholder="{{ placeholder }}" /></a>
        <input type="hidden" name="image" value="{{ image }}" id="input-image" />
    </div>
</div>
<textarea name="preview" id="preview" cols="200" rows="20">{{ text }}</textarea>